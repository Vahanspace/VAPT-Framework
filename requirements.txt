# Core runtime
PyYAML>=6.0
requests>=2.31
openpyxl>=3.1
jinja2>=3.1
jsonschema>=4.20

# Static analysis engines (optional but recommended; scanners degrade gracefully without them)
bandit>=1.7
pip-audit>=2.7

# Testing
pytest>=8.0
